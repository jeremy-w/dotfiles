[ui]
username = Jeremy W. Sherman
ssh = ssh -C

[defaults]
email = --diffstat --git

[extensions]
convert=
extdiff=
graphlog=
patchbomb=
# Record functions like git add -p, and is also used by qrecord with MQ.
record=
#MacOSXKeychain=/Library/Python/2.5/site-packages/hgext/hgkeychain.py
hgext.mq=
color=
# hg shelve is like git stash.
shelve=

[diff]
git=True
nodates=True

[email]
method = smtp
from = Jeremy W. Sherman <jeremyw.sherman@gmail.com>

[smtp]
tls = True
host =
username =
password =

[extdiff]
cmd.sbsdiff  = diff
opts.sbsdiff = -N -b --side-by-side --width=90
